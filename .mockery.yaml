with-expecter: true
dir: "{{.InterfaceDir}}/mocks"
filename: "mock_{{.InterfaceNameSnake}}.go"
outpkg: "mocks"
mockname: "{{.InterfaceName}}"
resolve-type-alias: False
issue-845-fix: True

packages:
  github.com/LushnikovSR/spaceship_factory/payment/internal/service:
    config:
      include-regex: ".*Service"

  github.com/LushnikovSR/spaceship_factory/inventory/internal/service:
    config:
      include-regex: ".*Service"

  github.com/LushnikovSR/spaceship_factory/inventory/internal/repository:
    config:
      include-regex: ".*Repository"

  github.com/LushnikovSR/spaceship_factory/order/internal/service:
    config:
      include-regex: ".*Service"

  github.com/LushnikovSR/spaceship_factory/order/internal/repository:
    config:
      include-regex: ".*Repository"

  github.com/LushnikovSR/spaceship_factory/order/internal/client/grpc:
    config:
      include-regex: ".*Client"